export const isAuthPage = (pathname: string) => /^\/auth\//.test(pathname);

export const isValidEmail = (email: string) => {
  const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
  return emailRegex.test(email);
};
